# 증거로 사용하는 기록의 무결성 설계

> 출처·최신판: https://github.com/YW-Rainbow/welfare-vibecoding-starter
> 이 문서는 바이브코딩으로 만든 전자결재 앱의 보안 감사에서 확인한 문제와 개선 방법을 정리했습니다.

## 기성 전자결재 제품과 자체 개발 앱의 차이

외부 전자결재 제품은 기록을 작성하는 기관과 기록을 보관하는 서비스 업체가 분리되어 있습니다. 기관의 사용자가 마음대로 DB를 수정할 수 없기 때문에 감사 과정에서 기록의 신뢰성을 설명하기 쉽습니다.

기관이 직접 만든 앱에서는 기관이 기록을 작성하고 같은 기관의 관리자가 DB 소유자 권한도 가질 수 있습니다. 관리자가 DB를 직접 수정할 수 있다면 애플리케이션 코드에 수정 방지 기능을 구현했더라도 기록이 변경되지 않았다는 사실을 별도로 증명해야 합니다.

## 개인정보 등급과 증거성을 별도로 판정합니다

데이터 등급 S/P/N은 개인정보의 민감도를 구분합니다. 기록 무결성은 해당 기록이 감사, 지도점검, 분쟁에서 증거로 사용되는지를 구분합니다. 두 기준은 서로 독립적입니다. 전자결재 문서는 P등급 개인정보만 포함하더라도 증거성은 매우 높을 수 있습니다.

**증거급 기록**에는 결재·품의, 근태(연차·시간외·출장), 회계·지출, 계약, 보조금 정산처럼 법정 보존 의무가 있거나 감사·지도점검·분쟁에서 효력을 확인해야 하는 기록이 포함됩니다.

| 기록 유형 | 바이브코딩 적용 기준 |
|---|---|
| 증거급 기록 | 검증된 기성 제품을 우선 사용합니다. 직접 개발한다면 아래 8개 원칙을 모두 적용하고 전문가 검토를 받습니다. |
| 업무 기록(일지·대장·명부) | 직접 개발할 수 있습니다. 최소한 원칙 1, 4, 7을 적용합니다. |
| 편의 기능(자원 예약·일정·안내·익명 통계) | 바이브코딩으로 시작하기에 적합합니다. |

전자결재는 상태 변경, 승인 흐름, 권한 관리 등을 학습하기 좋은 주제이지만 기록 무결성 요구가 높아 실제 운영용으로 만들기는 어렵습니다. 학습용 앱과 실제 근태·회계에 사용하는 앱을 구분하고, 운영 환경에는 무결성 요구가 낮은 기능부터 적용하세요.

## 증거급 기록에 적용할 8개 원칙

### 1. 사용자와 기록을 삭제하지 않고 상태를 변경합니다

퇴직자 계정을 삭제할 때 FK에 `ON DELETE CASCADE`가 설정되어 있으면 해당 사용자가 작성하거나 결재한 기록도 함께 삭제될 수 있습니다. 증거급 기록의 FK는 `RESTRICT`를 사용하고, 사용자 계정은 삭제하지 않고 `status = RETIRED` 같은 상태로 변경합니다. 기록의 보존기간이 사용자의 재직 기간보다 길 수 있기 때문입니다.

### 2. 본문 수정은 새 revision으로 저장합니다

결재 문서 객체 전체를 `UPDATE`할 수 있으면 결재가 끝난 뒤에도 휴가 일수나 금액을 바꿀 수 있습니다. 다음과 같이 데이터를 분리합니다.

- 문서 header: 문서 ID, 작성자, 생성 시각 등 변경하지 않는 정보
- 본문 revision: 수정할 때마다 새 행으로 추가하는 append-only 데이터
- event: 상신, 승인, 반려, 회수, 폐기 이력을 추가하는 append-only 데이터

결재자는 현재 문서 객체가 아니라 특정 revision과 해당 revision의 hash를 승인합니다.

### 3. 감사 로그를 append-only로 저장하고 hash chain을 적용합니다

현재 상태만 저장한 컬럼은 변경 이력을 설명할 수 없습니다. 상신, 승인, 반려, 회수, 폐기 작업을 별도의 감사 로그 테이블에 이벤트로 추가하고 `UPDATE`와 `DELETE`를 차단합니다.

각 이벤트에 이전 이벤트의 hash를 포함하면 중간 이벤트를 수정했을 때 이후 hash가 모두 달라집니다. 이를 통해 로그가 변경되었는지 확인할 수 있습니다.

### 4. 시각은 서버에서 생성합니다

클라이언트의 `new Date()`를 사용하면 사용자 PC의 시계를 변경해 결재 시각을 조작할 수 있습니다. DB에서 `TIMESTAMPTZ DEFAULT now()`로 시각을 생성하고, 클라이언트가 생성·수정 시각 컬럼을 직접 입력하지 못하게 합니다.

### 5. 외부 파일의 URL이 아니라 결재 시점의 내용을 고정합니다

날인 이미지를 현재 파일 URL로만 저장하면 같은 경로의 이미지를 교체했을 때 과거 문서의 도장도 바뀔 수 있습니다. 결재 시점의 파일을 별도로 복사하거나 content hash를 포함한 변경 불가능한 경로를 사용합니다. 파일 버킷은 private으로 설정합니다.

### 6. 문서 번호를 DB sequence로 발급하고 재사용하지 않습니다

`목록.length + 1`로 번호를 만들면 문서를 삭제하거나 동시에 상신할 때 번호가 충돌합니다. 감사에서는 문서 번호의 중복과 누락도 확인 대상이 됩니다. 연도별 DB sequence를 사용해 원자적으로 번호를 발급하고 한번 사용한 번호는 다시 사용하지 않습니다.

### 7. 조회와 출력도 접근 로그에 기록합니다

병가 사유 같은 자유 입력 필드에 진단명이 포함되면 근태 앱도 민감정보 처리 시스템이 됩니다. 이 경우 접속기록을 2년 이상 보관하고 위·변조 방지와 정기 점검을 적용해야 합니다.

조회, 수정, 삭제뿐 아니라 출력과 파일 내보내기도 로그에 기록합니다. 가능한 경우 병가 사유는 자유 텍스트 대신 사유 코드와 증빙 파일로 받으면 진단명이 본문에 불필요하게 쌓이는 것을 줄일 수 있습니다.

### 8. 관리자 권한으로 가능한 작업의 한계를 문서화합니다

단일 프로젝트의 소유자나 service role key를 가진 관리자를 기술적으로 완전히 차단할 수는 없습니다. 관리자는 DB trigger를 삭제하거나 RLS를 우회할 수 있습니다. 따라서 “관리자도 절대 삭제할 수 없다”고 설명해서는 안 됩니다.

목표는 삭제 자체를 불가능하게 만드는 것이 아니라 기록을 변경하거나 삭제했을 때 검증할 수 있는 외부 근거를 남기는 것입니다.

- 테이블 직접 쓰기 권한을 회수하고 지정된 함수나 API로만 수정하게 합니다.
- 매일 로그 또는 데이터 파일의 hash를 계산합니다.
- 계산한 hash를 이사장, 감사, 회계사 메일이나 별도 저장소처럼 DB 관리자 권한 밖의 위치로 전송합니다.
- DB를 과거 상태로 되돌렸더라도 외부에 보관된 전날의 hash와 비교해 변경 여부를 확인합니다.

## 보관 등급 0~5

보관 등급은 기록 유형별로 적용할 무결성 보호 수준을 정합니다. 직원 감시나 특정 행위자를 찾기 위한 제도가 아니라 기관과 직원이 같은 기록을 신뢰하고, 감사와 분쟁에서 관리 방법을 설명하기 위한 기준입니다.

### 적용 목적

1. 기관이 연말 실적이나 근태 기록을 내부에서 신뢰하고 사용할 수 있게 합니다.
2. 감사자에게 기록의 생성·수정·보관 방법을 설명할 수 있게 합니다.
3. 분쟁이 발생했을 때 기관과 직원 양쪽이 같은 기록을 근거로 사용할 수 있게 합니다.

무결성 기록이 기관 한쪽의 주장만 강화하는 수단이 되지 않도록 직원과 정보주체도 관련 기록을 확인할 수 있게 해야 합니다.

### 앱 로그의 한계

앱 코드에서 작성하는 로그는 앱을 거친 수정만 기록합니다. DB 콘솔에서 행을 수정하거나 service role key로 직접 `UPDATE`하면 앱 로그가 남지 않습니다. DB trigger를 사용하면 앱을 거치지 않은 수정도 기록할 수 있지만 DB 소유자는 trigger를 제거할 수 있습니다.

따라서 등급 2 이상에서는 로그의 양을 늘리는 것보다 데이터 또는 hash의 사본을 관리자 권한 밖에 보관합니다. 감사 로그는 누가 수정했는지 확인하는 데 사용하고, 외부 사본은 데이터가 변경되었다는 사실을 검증하는 데 사용합니다.

### 등급표

| 등급 | 구현 내용 | 운영 비용 | 확인할 수 있는 문제 | 적용 예시 |
|---|---|---|---|---|
| **0** | 별도의 무결성 보호를 적용하지 않음 | 없음 | 없음 | 자원 예약, 일정, 안내, 익명 통계 |
| **1** | 행을 삭제하지 않고 상태만 변경. 시각은 서버에서 생성. 구글 시트가 원본이면 버전 기록 사용 | 매우 낮음 | 실수로 인한 삭제와 일반적인 수정 | 일지·대장·명부 |
| **2** | 매일 데이터를 CSV로 내보내 날짜별 폴더에 자동 저장 | 배치 작업 하나 | DB를 나중에 수정하거나 삭제한 경우 | 감사·지도점검에서 원본 제출을 요구하는 기록 |
| **3** | CSV 저장 배치가 날짜와 행 수를 외부 메일로 전송 | 낮음 | CSV 파일을 삭제하거나 행 수를 바꾼 경우 | 실적 보고의 근거가 되는 기록 |
| **4** | 행 수와 함께 파일의 SHA-256 hash를 외부로 전송 | 낮음 | 행 수를 유지한 채 내용을 변경한 경우 | 근태·급여·회계처럼 금액과 시각을 확인해야 하는 기록 |
| **5** | 월별 확정본의 hash를 정보주체에게도 제공하고, DB trigger로 직접 수정도 기록 | 월 마감 절차 필요 | 기관 내부 관리자 권한으로 가능한 대부분의 변경 | 인건비 정산, 노무 분쟁 가능성이 있는 기록 |

등급 5보다 높은 수준이 필요하다면 자체 개발보다 검증된 기성 제품을 사용하는 편이 적절합니다. 직접 운영해야 할 절차가 자체 개발의 이점보다 커지기 때문입니다.

### 등급 선택 기준

1. **앱 전체가 아니라 테이블마다 등급을 정합니다.** 같은 앱 안에서도 근태 테이블은 4등급, 프로그램일지는 1등급일 수 있습니다.
2. **정상적으로 수정되는 테이블은 3등급 이상을 적용하지 않습니다.** 실적과 일지는 누락된 내용을 나중에 보완할 수 있습니다. 정상 수정마다 경고가 발생하면 담당자가 경고를 확인하지 않게 됩니다.
3. **낮은 등급부터 적용합니다.** 0등급에서 1등급으로 올릴 때 얻는 효과가 크며, 대부분의 사회복지 업무 앱은 1~2등급으로 시작할 수 있습니다.
4. **외부 사본은 정기적으로 비교하지 않고 문제가 생겼을 때 검증에 사용합니다.** 매일 수동 대조하는 절차를 추가하면 운영 부담만 늘어납니다.
5. **앱 개발자와 DB 소유자 계정을 분리합니다.** 별도 코드 없이 적용할 수 있고 관리자 한 사람에게 모든 권한이 집중되는 것을 줄입니다.

### 외부 검증 정보의 보관 위치

3등급 이상에서는 날짜, 행 수, hash를 DB 관리자 권한 밖에 보관합니다.

- **기관 로그 전용 메일함**: 기관 관리자의 권한 범위에 있을 수 있지만 실수로 수정하거나 삭제하는 문제를 줄일 수 있습니다.
- **Google Workspace 밖의 기관 명의 계정**: 관리 콘솔과 다른 권한으로 운영하고 비밀번호는 앱 담당자가 아닌 사람이 관리합니다.
- **정보주체 본인**: 근태와 급여 기록의 월별 hash를 직원에게 보내면 기관이 단독으로 수정할 수 없는 사본이 생깁니다. 급여명세서에 hash를 포함할 수 있습니다.
- **지자체 제출 문서**: 이미 제출하는 정산 서류에 해당 기간 데이터의 hash를 기록하면 접수 시점을 외부 문서로 확인할 수 있습니다.

SHA-256 hash는 원본 크기와 관계없이 64자의 문자열입니다. 원본 개인정보를 포함하지 않으므로 외부 검증용으로 전달할 수 있습니다. 원본 CSV 자체를 외부 메일로 보내지 말고 날짜, 행 수, hash만 전송하세요.

매일 별도의 hash를 외부에 전송한다면 hash chain은 필수 사항이 아닙니다. 반대로 외부 전송 주기가 길다면 이전 hash를 다음 파일에 포함하는 hash chain을 추가해 중간 기간의 변경을 확인할 수 있습니다.

## 전체 데이터가 삭제된 경우

관리자 권한을 가진 사람이 의도적으로 모든 데이터를 삭제하는 상황을 기술만으로 완전히 막을 수는 없습니다. 다음 방식으로 탐지와 복구를 준비합니다.

- 데이터가 사라지면 사용자와 관리자가 즉시 확인할 수 있도록 모니터링합니다.
- 등급 2의 날짜별 CSV를 백업으로 사용해 DB를 복구합니다.
- 삭제 이벤트와 외부에 남은 hash를 근거로 사고 시점과 범위를 확인합니다.

실제 현장에서는 악의적인 삭제보다 퇴사자 계정을 정리하면서 드라이브를 삭제하거나, 요금제를 변경하다 프로젝트가 중단되거나, 시트를 잘못 정렬해 행 관계가 어긋나는 사고가 더 자주 발생할 수 있습니다. requirements.md의 백업 요건과 `ops/handover.md`의 기관 공용 계정 원칙을 함께 적용하세요.

무결성은 기록이 모르게 변경되지 않았음을 확인하는 기준이고, 백업은 기록을 잃었을 때 복구하는 기준입니다. 두 요구사항을 별도로 설계하고 테스트해야 합니다.

## 공개 구현 사례

[SW_EDMS](https://github.com/SWJoong/SW_EDMS)는 바이브코딩으로 만든 전자결재 학습용 샘플입니다. 보안 감사를 받은 뒤 실제 근태·회계 기록에 바로 사용할 수 없다는 한계를 README에 공개하고, hash chain, append-only 테이블, 서버 시각, 접근 로그를 포함한 무결성 강화 스키마와 A/B/C 무결성 등급표를 추가했습니다.

SW_EDMS의 A/B/C 구분은 이 문서의 증거급 기록, 업무 기록, 편의 기능 구분과 대응합니다. 직접 구현하기 전에 스키마와 감사 결과를 참고할 수 있습니다.

## 최종 확인

증거급 기록을 처리하는 앱을 운영에 도입하기 전에 다음 질문에 답해야 합니다.

> 감사자가 “이 기록이 승인 이후 변경되지 않았다는 것을 어떻게 확인할 수 있습니까?”라고 물었을 때 어떤 로그, hash, 외부 사본으로 설명할 것인가?

답을 문서와 테스트 결과로 제시할 수 없다면 검증된 기성 제품을 사용하세요.
