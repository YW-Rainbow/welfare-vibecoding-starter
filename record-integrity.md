# 기록 무결성 — 증거가 되는 기록은 다르게 다룬다

> 출처·최신판: https://github.com/YW-Rainbow/welfare-vibecoding-starter
> 실제 바이브코딩 전자결재 앱 보안감사에서 나온 권고를 원칙으로 증류한 문서입니다.

## 핵심 통찰 하나

외부 전자결재 업체의 문서가 지도점검에서 통과되는 이유는 기술이 뛰어나서가 아닙니다.
**기록을 만드는 주체와 기록을 보관하는 주체가 분리되어 있기 때문**입니다.

기관이 직접 만든 앱은 이 분리가 없습니다. 기관이 기록을 만들고, 기관이 보관하고,
기관 관리자가 DB를 만질 수 있습니다. 아무리 코드를 잘 짜도 "당사자가 자기 기록을
고칠 수 있는 구조"라는 사실은 남고, 감사자는 정확히 그걸 봅니다.

## 두 번째 판정 축: 이 기록은 증거인가?

이 리포의 데이터 등급(S/P/N)은 "얼마나 비밀인가"를 잽니다. 그것과 별개로
"이 기록이 증거인가"를 물어야 합니다. 두 축은 독립적입니다 — 전자결재 문서는
개인정보로는 평범해도(P등급), 증거로는 최고 등급입니다.

**증거급 기록**: 결재·품의, 근태(연차·시간외·출장), 회계·지출, 계약,
보조금 정산 — 법정 보존 의무가 있고, 감사·지도점검 대상이며, 분쟁 시 효력을 다투는 기록.

**판정:**

| 기록 성격 | 바이브코딩 |
|---|---|
| 증거급 (위 목록) | **비권장 — 검증된 기성 도구 사용.** 자체 제작 시 아래 8원칙 전부 + 전문가 검토가 전제 |
| 업무 기록 (일지·대장·명부) | 가능 — 8원칙 중 1·4·7 적용 |
| 편의 기능 (자원 예약, 일정, 안내, 익명 통계) | 자유롭게 — 바이브코딩 최적 영역 |

**역설을 숨기지 말 것**: 전자결재는 바이브코딩 학습 소재로 매력적이지만, 하필
기록 무결성 요구가 가장 높은 영역이라 자체 제작 난이도가 최상위입니다. 배우기 좋은
주제와 실전 투입해도 되는 주제는 다릅니다. 연습은 전자결재로 하되, 기관에 실제로
붙이는 건 무결성 요구가 낮은 영역부터.

## 그래도 기록성 앱을 만든다면 — 8원칙

실제 감사에서 발견된 사고 유형을 원칙으로 뒤집은 것입니다.

**1. 지우지 말고 상태를 바꿔라.**
FK에 `ON DELETE CASCADE`를 걸면 퇴직자 계정 정리 한 번에 그 사람이 기안·결재한
이력이 소리 없이 사라집니다. FK는 RESTRICT, 사람은 삭제 대신 `status = RETIRED`.
기록의 생애는 그 사람의 재직 기간보다 깁니다.

**2. 기록은 고쳐 쓰는 것이 아니다.**
문서 객체를 통째로 UPDATE하는 구조에서는 결재 완료 후에도 휴가 일수·금액을
바꿔치기할 수 있습니다. 헤더(불변) / 본문 리비전(append-only) / 이벤트(append-only)로
나누고, 결재자가 승인하는 대상을 "문서"가 아니라 "그 해시를 가진 리비전"으로.

**3. 로그는 별도 그릇에, 체인으로.**
상태 컬럼은 로그가 아닙니다. 상신·승인·반려·회수·폐기를 전부 이벤트로 쌓는
append-only 감사 로그 테이블을 두고, UPDATE/DELETE를 차단하고, 직전 행의 해시를
물려 체인을 만들면 — 중간 한 줄만 손대도 이후 구간이 전부 깨져서 드러납니다.

**4. 시각은 서버가 정한다.**
클라이언트의 `new Date()`로 결재 시각을 저장하면 PC 시계만 돌려도 결재 일시가
바뀝니다. 모든 시각은 `TIMESTAMPTZ DEFAULT now()`, 클라이언트는 시각 컬럼에
값을 넣을 수 없게. 수정 비용은 낮고 효과는 큰 항목.

**5. 참조 말고 내용을 고정하라.**
날인 이미지를 URL로 저장하면, 저장소의 같은 경로에 다른 이미지를 덮어쓰는 순간
DB는 한 글자도 안 바뀐 채 과거 결재 문서의 도장이 바뀝니다. 결재 시점의 바이트를
복사하거나 콘텐츠 해시 기반 경로를 쓰고, 버킷은 당연히 private.

**6. 번호의 연속성이 증거다.**
`목록.length + 1` 채번은 삭제 한 건에 번호가 충돌하고 동시 상신에 중복됩니다.
감사에서 문서번호 결번은 "여기 있던 문서는 어디 갔나"라는 질문 그 자체입니다.
연도별 DB 시퀀스로 원자적 발급, 번호 재사용 금지.

**7. 조회도 기록이다.**
병가 사유 같은 자유 텍스트에는 진단명이 들어옵니다 — 그 순간 민감정보(건강)
처리 시스템이 되고, 접속기록 보관 의무가 기본 1년에서 **2년 이상**으로 올라가며
위변조 방지·정기 점검(현행 월 1회) 의무가 따라옵니다. 조회·출력 로그를 만들고, 애초에 사유는 자유 텍스트 대신
코드값 + 증빙 첨부로 받아 본문에 진단명이 쌓이지 않게.

**8. "관리자도 못 지운다"고 주장하지 마라.**
단일 프로젝트에서 소유자를 기술로 완전히 막는 것은 불가능합니다. 트리거는
DROP되고, 관리자 키는 정책을 우회합니다. 이걸 "막았다"고 주장하면 점검에서
오히려 신뢰를 잃습니다. 목표를 바꾸세요 — **"지우면 지운 흔적이 남는다"로.**
테이블 직접 쓰기 권한을 회수해 지정된 함수로만 쓰게 좁히고, 매일 로그 체인의
머리 해시를 외부(이사장·감사·회계사 메일, 별도 저장소)로 자동 발송합니다.
DB를 통째로 되돌려도 외부에 남은 어제자 해시와 대조하면 드러납니다.
— 이것이 "보관 주체 분리"라는 외부 업체의 신뢰 구조를 기관이 흉내 낼 수 있는
유일한 방법입니다.

## 마지막 점검 한 줄

기록성 앱을 결재에 올리기 전에 스스로에게 물으세요:
**"지도점검 나온 감사자에게 '이 기록은 만든 사람이 고칠 수 없습니다'를
어떻게 증명할 것인가?"** — 이 질문에 답이 없으면 기성 도구를 쓸 때입니다.
