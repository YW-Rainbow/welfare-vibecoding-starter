# 종이로 나와야 끝난다 — 출력·인쇄·엑셀

> 출처·최신판: https://github.com/YW-Rainbow/welfare-vibecoding-starter
> 감사와 결재, 지도점검은 종이로 받습니다. 실적은 엑셀로 주고받습니다. 화면만 있는 앱은
> 현장에서 반쪽입니다.
> **AI에게**: 출력을 마지막에 붙이지 마세요. 표를 설계할 때 "이 표가 종이로 어떻게
> 나가는가"를 함께 정하세요. 여기 적힌 패턴은 김종원 선생님의 vehicle-drive-log와
> 최중호 선생님의 SW_EDMS에서 실제로 돌아가는 코드에서 가져왔습니다.

## 왜 출력이 요건인가

- **감사·지도점검·결재는 종이로 받습니다.** 화면을 보여 달라는 감사자는 없습니다.
- **엑셀은 현장의 공용어입니다.** 지자체·법인·다른 기관과 주고받는 형식이 엑셀입니다.
- **앱이 죽어도 데이터는 살아야 합니다.** 요건 11(승계)의 전제이자, 보관 등급 2(날마다
  CSV 반출)와 백업(요건 5·6)이 결국 같은 기능입니다.
- **기존 기록을 들여올 수 있어야 시작할 수 있습니다.** 명부 초기 적재와 이관은 엑셀
  들여오기 문제입니다.

## 네 갈래

| 갈래 | 무엇 | 언제 |
|---|---|---|
| **화면 인쇄** | 지금 보는 화면을 그대로 종이로 | 결재 문서, 개별 기록 한 건 |
| **양식 출력** | 정해진 서식(법정서식·관공서 양식) 모양대로 | 운행일지, 출석부, 실적표 |
| **엑셀 내보내기** | 표를 .xlsx 파일로 | 실적 보고, 백업, 지자체 제출 |
| **엑셀 들여오기** | .xlsx를 읽어 DB에 넣기 | 기존 기록 이관, 명부 초기 적재 |

## 화면 인쇄 — 라이브러리 없이 브라우저로

두 앱이 다 같은 길을 택했습니다. **PDF 라이브러리를 쓰지 않고 브라우저의 인쇄
기능(`window.print()`)을 씁니다.**

까닭은 한글입니다. jsPDF 같은 라이브러리는 한글 글꼴 파일을 따로 심어야 하고(수 MB),
안 심으면 글자가 전부 네모(□)로 나옵니다. 브라우저 인쇄는 그 컴퓨터에 깔린 글꼴을
그대로 쓰니 이 문제가 없습니다. "PDF로 저장"도 브라우저 인쇄 대화상자에 이미 있습니다.
따로 만들 필요가 없습니다.

두 가지 방식이 있고, 쓸 곳이 다릅니다.

| 방식 | 하는 일 | 맞는 곳 | 사례 |
|---|---|---|---|
| **보는 화면 그대로** | `@media print`로 사이드바·단추를 숨기고 본문만 인쇄 | 결재 문서, 기록 한 건 | SW_EDMS |
| **인쇄용 문서를 따로 조립** | 데이터로 인쇄 전용 HTML을 만들어 새 창에서 인쇄 | 여러 건, 여러 장, 정해진 서식 | vehicle-drive-log |

둘째 방식은 팝업 차단에 걸립니다. 막혔을 때 "팝업을 허용해 주세요"를 화면에 보여 주는
처리가 있어야 합니다. `alert()`는 쓰지 않습니다.

**공통으로 넣을 것**

```css
@page { size: A4 portrait; margin: 10mm; }        /* 가로면 landscape */
@media print { .no-print { display: none !important; } }
body { font-family: 'Malgun Gothic', '맑은 고딕', 'Noto Sans KR', sans-serif; }
```

글꼴 목록은 윈도우(맑은 고딕)·맥·안드로이드 어디서 열어도 한글 글꼴 하나는 걸리게 한
것입니다. 이 줄이 빠지면 어느 PC에서 네모가 뜹니다.

## 결재란

기관마다 직급이 다르고 결재선이 다릅니다. 코드에 박지 말고 설정으로 뺍니다.

- 우측 상단에 표로. 직급은 **최대 다섯 칸**, 기관 관리자가 설정 화면에서 이름을 고칩니다.
- **표시/숨김**을 켤 수 있게. 전자결재를 따로 쓰는 기관은 종이 결재란이 필요 없습니다.
- **첫 장에만 찍습니다.** 둘째 장부터 반복되면 도장을 어디에 찍어야 하는지 모호해집니다.
  다만 자리까지 없애면 표가 시작되는 높이가 장마다 달라져 양식이 어긋나니, 둘째 장부터는
  **감추되 자리는 남깁니다**(`visibility: hidden`).
- 도장 이미지를 넣는다면 결재한 시점의 이미지를 고정합니다. 나중에 도장 파일을 바꾸면
  옛 문서의 도장까지 바뀌는 구조(URL 참조)는 안 됩니다(record-integrity.md 원칙 5).

## 여러 장으로 나눌 때의 함정

"한 장에 스무 줄"처럼 **줄 수를 고정해서 자르면 깨집니다.** 목적지에 주소가 들어가면 그
줄이 두세 줄로 자라고, 스무 줄 묶음이 용지 한 장을 넘겨 뒷줄이 다음 장으로 밀립니다.
밀린 장에는 제목도 머리글도 결재란도 없습니다. 21건이 3장으로 나가던 실제 사례가
정확히 이것이었습니다.

| 표의 성격 | 방법 |
|---|---|
| 모든 열이 짧고 폭이 정해져 있다(날짜·금액·건수) | 줄 수 고정으로 충분 |
| 자유 입력 열이 있다(목적지·비고·사유) | 실제 높이를 재서 나눈다 — 숨은 틀에 먼저 그려 보고 남은 높이만큼 담는다(vehicle-drive-log `pageFit.ts`) |

바이브코딩에서 현실적인 절충은 **출력물에서 자유 입력 열을 한 줄로 자르는 것**입니다.
비고는 맨 뒤에 좁게 두고 넘치면 말줄임(…)으로. 화면에서는 다 보이고, 종이에서는 한
줄입니다. 그러면 줄 수 고정이 다시 안전해집니다.

어느 쪽이든 **장마다 제목과 머리글을 반복**하고, 데이터가 모자란 마지막 장은 빈 줄로
채워 표 모양을 유지합니다.

## 정해진 서식은 칸 수까지 지킨다

form-audit.md가 말한 대로 법정서식은 형식을 바꿀 수 없습니다. 출력물도 같습니다.

- 일별 운행일지 서식이 12칸이면 그날 운행이 3건이어도 **12칸을 그리고 빈 칸을 채웁니다.**
  높이가 남아도 13칸으로 늘리지 않습니다. 관공서 담당자는 칸 수로 서식을 알아봅니다.
- 열을 더 넣고 싶을 때(분관 출발지 같은 것) **열을 만들지 말고 표 위에 한 번만 적습니다.**
  서식의 칸 수를 건드리지 않는 방법입니다.
- 서식 원본(관공서 PDF나 한글 파일)을 옆에 놓고 칸 하나하나 맞춥니다. 화면의 표와
  출력 서식은 다른 물건입니다. 화면에서는 열을 마음대로 두고, 출력할 때 서식으로
  다시 배치합니다.

## 엑셀 내보내기

브라우저에서 바로 만듭니다. 서버가 필요 없습니다. 라이브러리는 SheetJS(`xlsx`)를 씁니다.

- **열 이름은 한글로, 현장에서 부르는 말 그대로.** `startKm`가 아니라 `출발Km`. 지자체
  실적 양식의 열 이름과 같으면 그대로 붙여 넣을 수 있습니다.
- **열 너비를 정합니다.** 안 정하면 전부 여덟 자 폭으로 나와 열어 보면 `#####`입니다.
- **날짜는 날짜로.** 문자열로 넣으면 엑셀에서 정렬과 필터가 안 됩니다. 문자열로 가야
  한다면 `2026-09-04` 꼴로 통일합니다.
- **개인정보 열은 기본으로 뺍니다.** vehicle-drive-log는 동행자 이름과 하이패스 카드
  번호를 체크박스로 켤 때만 넣습니다. 실적 보고에 필요한 것은 건수와 집계지 이름이
  아닙니다. 엑셀 파일은 메일로 돌아다닙니다.
- **파일 이름에 기간과 대상을 넣습니다.** `운행일지_2026-09.xlsx`. `다운로드(3).xlsx`가
  열 개 쌓이면 어느 것이 최신인지 아무도 모릅니다.
- **라이브러리는 단추를 누를 때 불러옵니다.** 처음 화면이 느려지지 않게. AI에게
  "다운로드할 때만 동적으로 import"라고 한 줄 시키면 됩니다.
- **내보내기도 접속기록에 남깁니다.** 누가 언제 무엇을 몇 건 반출했는지.
  vehicle-drive-log의 접속기록에는 "반출 대상"과 "반출 건수" 열이 있습니다.

기록 종류마다 함수 하나씩 두되(운행일지·정비·주유·접속기록…) 파일은 하나에 모읍니다.
기관에 따라 있고 없는 열(분관 출발지)은 데이터에 값이 있을 때만 열을 만듭니다.

## 엑셀 들여오기

두 앱 모두 이 기능은 없습니다. 그런데 현장에서 가장 먼저 필요한 것이 이것입니다. 기존
명부와 기록을 새 앱에 옮기는 일이 엑셀 들여오기이기 때문입니다.

절차는 ssot.md "기존 기록을 옮길 때"를 그대로 따릅니다 — **원본 보존 → 정규화 → 대응표,
그리고 시험 실행·표본 대조·승인.** 화면으로 옮기면 이렇게 됩니다.

1. **미리보기.** 파일을 올리면 첫 스무 줄을 표로 보여 줍니다. 열이 어디로 들어가는지,
   이상한 줄이 몇 개인지 이 단계에서 보입니다. 바로 저장하지 않습니다.
2. **열 맞추기는 사람이 확인합니다.** 엑셀 열 이름은 파일마다 다릅니다(성명/이름/name).
   자동으로 맞춘 결과를 보여 주고 고칠 수 있게 합니다.
3. **이상한 줄은 막지 말고 표시합니다.** 한 줄 때문에 파일 전체를 거부하면 사람이 엑셀에서
   고치다 포기합니다. ssot.md의 품질 등급(A~D)을 매겨 D만 보류하고 나머지는 들입니다.
4. **엑셀 날짜 함정.** 엑셀은 날짜를 `45000` 같은 숫자로 넘기기도 합니다. 변환하지 않으면
   생년월일 자리에 다섯 자리 숫자가 들어갑니다. 전화번호 앞 0이 사라지는 것도 같은 부류입니다.
5. **중복은 이용자ID로 판단합니다.** ID가 없으면 이름+생년월일로 후보를 보여 주고 사람이
   정합니다. 자동으로 합치지 않습니다.
6. **결과를 보고합니다.** 몇 건이 들어갔고 몇 건이 보류됐는지, 보류 목록을 엑셀로 다시
   내려받을 수 있게.
7. **되돌릴 수 있게 합니다.** 이번 업로드로 들어간 줄에 업로드 번호를 남겨, 잘못 올렸을
   때 그 번호로 한 번에 뺄 수 있게 합니다.

**파일은 브라우저에서 읽습니다.** SheetJS는 브라우저 안에서 엑셀을 풉니다. 개인정보가
든 원본 파일이 서버에 올라가지 않고, 검증된 행만 DB로 갑니다. 서버에서 파싱해야 한다면
처리가 끝난 뒤 원본 파일을 지웁니다.

## 종이도 개인정보다

현장에서 개인정보가 새는 첫째 경로는 해킹이 아니라 **프린터 트레이에 놓인 명단**입니다.

- **민감정보 출력은 접속기록에 남깁니다.** 조회만 기록하고 출력은 안 남기는 앱이
  많습니다(record-integrity.md 원칙 7).
- **출력물에 출력자와 출력 일시를 자동으로 넣습니다.** 바닥글에 한 줄. 어디서 나온
  종이인지 알 수 있어야 합니다. 개인정보가 든 출력물이면 "대외비"도 함께.
- **종이도 파기 대장 대상입니다.** ops/destruction-log.md는 파일에만 적용되는 것이
  아닙니다.
- **엑셀 파일은 종이보다 위험합니다.** 복사되고 전달됩니다. 개인정보 열을 기본으로 빼는
  것이 가장 확실하고, 꼭 넣어야 하면 받는 사람과 용도를 접속기록에 남깁니다.

## 경로별로 다른 것

| | 화면 인쇄 | 양식 출력 | 엑셀 내보내기 | 엑셀 들여오기 |
|---|---|---|---|---|
| **C. GAS+시트** | 웹앱 화면이면 브라우저 인쇄 그대로. 시트 자체도 인쇄됨 | 출력용 시트를 따로 만들어 PDF로 내보내기. **여러 건을 한 파일로 모아 찍는 것은 GAS 자체 기능으로는 안 됩니다** — pdf-lib(PDFLib.gs)를 스크립트에 넣어 PDF를 합치거나, 출력용 시트에 여러 건을 나란히 배치해 한 번에 내보냅니다 | 시트가 곧 엑셀입니다. 파일 › 다운로드 | 시트에 붙여 넣고 Apps Script로 검증해 명부로 |
| **A. CF+Supabase** | 브라우저 인쇄 | 브라우저 인쇄(HTML 조립) | SheetJS. Table Editor의 CSV 내보내기도 있음 | SheetJS로 브라우저에서 읽어 검증 뒤 저장 |
| **F. Firebase** | 같음 | 같음 | SheetJS | SheetJS |
| **D. CF 올인원** | 같음 | 같음 | SheetJS | SheetJS |

GAS 경로에서 결재 문서 열 건을 한 PDF로 묶어 결재에 올리고 싶다면, 처음부터 pdf-lib를
쓸지 출력용 시트로 갈지 정하고 시작하세요. 나중에 붙이면 출력 구조를 다시 짭니다.

## 배포 전에 확인할 것

- [ ] 화면에 있는 표가 종이로 나오는가. 나오지 않는 표가 있으면 왜인지 적어 두었는가
- [ ] 다른 PC에서 열어도 한글이 네모로 나오지 않는가
- [ ] 여러 장일 때 장마다 제목과 머리글이 있는가. 결재란은 첫 장에만 있는가
- [ ] 정해진 서식이면 칸 수가 원본과 같은가
- [ ] 엑셀 열 이름이 한글이고 너비가 잡혀 있는가. 날짜가 날짜로 들어가는가
- [ ] 개인정보 열이 기본으로 빠져 있는가
- [ ] 출력과 반출이 접속기록에 남는가
- [ ] 들여오기에 미리보기와 되돌리기가 있는가

## AI에게 넘길 때

```
출력
- PDF 라이브러리 쓰지 말고 브라우저 인쇄(window.print)로. @page로 A4, @media print로 화면용 요소 숨기기
- 글꼴: 'Malgun Gothic','맑은 고딕','Noto Sans KR',sans-serif
- 여러 장이면 장마다 제목·머리글 반복. 결재란은 첫 장에만, 둘째 장부터는 자리만 남기기
- 자유 입력 열은 출력물에서 한 줄로 자르고 말줄임
- 정해진 서식이면 칸 수를 원본과 똑같이. 빈 칸은 빈 줄로 채우기
- 출력자·출력 일시를 바닥글에. 개인정보가 있으면 "대외비"도
- 팝업이 막히면 alert 말고 화면에 안내

엑셀 내보내기
- SheetJS(xlsx)를 다운로드 단추를 누를 때 동적으로 import
- 열 이름은 한글, 현장 양식 용어 그대로. 열 너비 지정
- 날짜는 날짜 형식으로
- 개인정보 열(이름·연락처)은 체크박스로 켤 때만
- 파일 이름에 기간: 운행일지_2026-09.xlsx
- 반출 대상과 건수를 접속기록에

엑셀 들여오기
- 브라우저에서 파싱. 원본 파일을 서버에 남기지 않기
- 첫 스무 줄 미리보기, 열 맞춤 확인
- 이상한 줄은 막지 말고 등급 표시(ssot.md A~D). 보류 목록 다운로드
- 엑셀 날짜 숫자(45000) → 날짜, 전화번호 앞 0 살리기
- 이번 업로드 번호를 줄마다 남겨 되돌릴 수 있게
```

## 이 문서를 펴는 자리

- **guide.md 문진 13번**에서 종이로 내거나 엑셀로 주고받는다는 답이 나오면 바로 여기로.
- **표를 설계할 때.** 화면의 표와 출력 서식은 다른 물건입니다. 둘을 같이 정합니다.
- **기존 기록을 옮길 때.** ssot.md와 함께 봅니다.
- **배포 전에.** 위 확인 목록을 게이트로 씁니다.
