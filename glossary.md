# 용어사전 — 개발을 몰라도 읽을 수 있게

> 출처·최신판: https://github.com/YW-Rainbow/welfare-vibecoding-starter
> 이 리포의 다른 문서에서 쓰인 용어를 전부 모아 풀었습니다.
> **AI에게**: 사용자에게 용어를 말할 때 이 사전의 풀이를 사용하세요.
> 전문용어를 설명 없이 나열해 사용자를 압도하지 마세요.

## 기본 그림

- **프론트엔드**: 사용자 눈에 보이는 화면. 기관으로 치면 접수창구.
- **백엔드**: 화면 뒤에서 실제 일을 처리하는 부분. 사무실 안쪽.
- **서버**: 앱을 24시간 손님에게 차려 주는 컴퓨터. 남의 서버를 빌려 쓰는 게 **클라우드**.
- **배포**: 내 컴퓨터에서 만든 앱을 인터넷에 올려 다른 사람도 쓸 수 있게 하는 일.
- **API**: 프로그램끼리 서로 요청하고 답하는 창구. "민원 접수 창구의 양식"이라고
  생각하면 됩니다 — 정해진 양식으로 물으면 정해진 형식으로 답이 옵니다.
- **리전(region)**: 내 데이터가 실제로 놓이는 데이터센터의 지역. "서울 리전"이면
  물리적으로 국내에 저장된다는 뜻 (단, 해외에서 조회될 수 있는지는 별개 — 국외이전 항목 참고).
- **콜드스타트**: 한동안 안 쓰던 앱의 첫 응답이 느린 현상. 식은 주방에 첫 주문이 들어온 상황.

## 데이터와 DB

- **DB(데이터베이스)**: 표(테이블)들을 모아둔 전자 서류함. 구글시트와 비슷하지만
  훨씬 크고 빠르고, 여러 명이 동시에 써도 안 꼬입니다.
- **행(row) / 테이블**: 표의 한 줄 / 표 하나. "연 10만 행" = 1년에 기록 10만 줄.
- **SQL**: 표 형태의 DB에게 질문하는 언어. "작년 프로그램별 참여 인원을 세어줘"를
  한 문장으로 물을 수 있습니다. Supabase·Cloud SQL이 이 방식.
- **NoSQL(문서형)**: 표 대신 서류 뭉치(문서)로 저장하는 방식. 낱장 찾기는 빠르지만
  "여러 해를 가로지르는 질문"이 어렵습니다. Firestore가 이 방식.
- **종단분석**: 여러 해의 데이터를 관통해서 보는 분석. 이용자별 3년 추이, 연차 실적 비교 등.
- **시드(seed) 데이터**: 연습·개발용 가짜 데이터. 실제 이용자 정보 대신 넣고 테스트합니다.
- **마이그레이션**: DB의 구조(표·컬럼)를 바꾸는 작업, 또는 데이터를 다른 곳으로 옮기는 일.
- **TTL**: 유통기한. 기한이 지난 데이터를 시스템이 알아서 지우게 하는 설정.

## 파일 저장

- **버킷(bucket)**: 사진·파일을 담는 클라우드 폴더.
- **private / public 버킷**: 잠긴 폴더 / 주소만 알면 누구나 보는 폴더.
  이용자 사진은 반드시 private.
- **signed URL**: 유효기간이 있는 1회용 열람권. "1시간짜리 출입증을 끊어 준다"에 가깝습니다.
- **base64**: 파일을 글자로 바꿔 저장하는 방식. 사진을 이 방식으로 DB에 넣는 것은
  서류함에 앨범을 통째로 구겨 넣는 격이라 금지합니다.
- **OPFS / 로컬 퍼스트**: 서버 없이 사용자의 기기(브라우저) 안에만 저장하는 방식.
  서버가 없으면 유출될 서버도 없습니다. 단, 기기를 잃으면 데이터도 잃습니다.

## 열쇠와 권한

- **API 키 / 토큰**: 프로그램용 출입증. **비밀번호처럼 다뤄야 하고**, 코드나 공개
  저장소에 적어두면 안 됩니다.
- **anon 키 / service_role 키** (Supabase): 손님용 출입증 / **마스터키**.
  마스터키는 모든 잠금을 무시하니, 화면 쪽 코드에는 절대 넣지 않습니다.
- **RLS(행 단위 보안)**: 표의 **줄마다** "누가 볼 수 있나"를 DB가 직접 검사하는 잠금.
  담당자는 자기 담당 이용자 줄만 보이게 만드는 장치입니다.
- **deny-by-default(기본 거부)**: 허락 규칙이 없으면 아무도 못 보는 상태를 기본값으로.
  "명단에 없으면 출입 불가"와 같은 원칙.
- **IDOR**: 주소창의 번호만 바꿔서(?id=3 → ?id=4) 남의 기록을 보는 침입.
  로그인했다고 다 보여주면 생기는 구멍입니다.
- **XSS**: 입력창에 악성 코드를 심어 다른 사용자의 화면에서 실행시키는 공격.
- **FK(외래키) / ON DELETE CASCADE / RESTRICT**: 표끼리의 연결 고리 /
  연결된 기록까지 자동으로 함께 삭제 / 연결된 기록이 있으면 삭제 금지.
  결재·이력처럼 지워지면 안 되는 기록에는 RESTRICT.
- **해시**: 내용을 요약한 지문. 내용이 한 글자만 바뀌어도 지문이 완전히 달라져서,
  "바뀌지 않았음"을 증명하는 데 씁니다.

## 개발 도구

- **터미널 / CLI**: 마우스 대신 글자로 컴퓨터에 명령하는 창 / 그 명령 도구.
- **git / 저장소(repo) / 커밋**: 변경 이력이 전부 남는 코드 보관함 / 그 보관함 하나 /
  "여기까지 저장" 도장 한 번. 누가 언제 무엇을 왜 바꿨는지 남습니다.
- **클론(clone)**: 저장소를 내 컴퓨터로 통째로 복사해 오는 것.
- **clasp**: 구글 앱스 스크립트(GAS) 코드를 내 컴퓨터로 가져오고 올리는 구글 공식 도구.
- **wrangler**: Cloudflare에 배포할 때 쓰는 명령 도구.
- **에이전트형 AI 도구**: 대화만 하는 AI가 아니라, 내 컴퓨터의 파일을 고치고 명령을
  실행하고 배포까지 하는 AI. Claude Code, Codex 등.
- **CLAUDE.md / AGENTS.md**: 에이전트형 AI가 일 시작 전에 자동으로 읽는 규칙 파일.
  프로젝트 폴더 맨 위에 두면 "우리 기관의 작업 수칙"이 됩니다.
- **환경변수 / .env**: 키·비밀번호를 코드 밖에 따로 보관하는 파일. 공개 금지 목록 1순위.

## 법·제도 (자세한 내용은 requirements.md, ops/consent-notes.md)

- **개인정보 / 민감정보**: 누군지 알 수 있는 정보 / 그중 건강·장애·종교·범죄경력 등
  더 강하게 보호되는 정보. 상담기록 한 줄에도 민감정보가 들어갑니다.
- **국외이전**: 개인정보가 해외로 제공·위탁·보관되는 것. **해외에서 조회만 가능해도**
  해당할 수 있습니다. 동의를 받거나 처리방침에 공개하는 등 법정 근거가 필요합니다.
- **수탁자 / 위탁**: 우리 기관 일(개인정보 처리)을 대신 해주는 업체 / 그 관계.
  클라우드 회사가 대표적. 처리방침에 이름을 올려야 합니다.
- **DPA**: 수탁자(클라우드 회사)와 맺는 개인정보 처리 계약. 서명 절차가 따로 있는
  경우가 있으니 확인.
- **처리방침**: 기관이 개인정보를 어떻게 다루는지 공개하는 문서. 홈페이지 게시가
  일반적이고, 없으면 기관 내 게시·서면 비치로도 가능.
- **접속기록**: 누가 언제 어떤 개인정보를 봤는지의 기록. 민감정보 시스템은
  **2년 이상** 보관 의무 (자세히: requirements.md 요건 8).
- **CSAP**: 공공기관용 클라우드 보안 인증. 공공 소속이면 필수, 위탁·민간은
  계약·지침 확인 (자세히: ops/consent-form-sample.md 참고 5).
- **지도점검**: 감독기관이 시설 운영·기록을 살피러 오는 것. record-integrity.md의
  출발점이 되는 장면입니다.
